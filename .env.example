# SQL Server — Windows Authentication, no credentials required.
DB_SERVER=James-desktop\sqlexpress
DB_NAME=InvestmentForecaster
PORTFOLIO_DB_NAME=InvestmentPortfolio
DB_DRIVER=ODBC Driver 17 for SQL Server

# Anthropic API
ANTHROPIC_API_KEY=your-api-key-here

# IBKR Client Portal Gateway (primary market data source)
# Leave as default if running on the same machine as TWS / Client Portal Gateway.
IBKR_GATEWAY_URL=https://localhost:5000
